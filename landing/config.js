/**
 * AI快侠 API 地址配置
 *
 * 部署后端到 Render 后，把下面的 URL 改成你的实际地址，例如：
 *   https://aikuaixia-api.onrender.com/api/v1
 *
 * 留空则：GitHub Pages 访问时用下方 DEFAULT_API，本地开发用 localhost
 */
window.AIKUAIXIA_API_BASE = '';

// Render 部署后的默认 API（部署完成后在此填写，或设 AIKUAIXIA_API_BASE）
window.AIKUAIXIA_DEFAULT_API = 'https://aikuaixia-api.onrender.com/api/v1';
